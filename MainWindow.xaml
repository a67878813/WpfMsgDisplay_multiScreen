<Window x:Class="WpfMsgDisplay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        

        xmlns:local="clr-namespace:WpfMsgDisplay"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Loaded="Window_Loaded"
        
        xmlns:tb="http://www.hardcodet.net/taskbar" Background="#FF878787"
        >

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="47*"/>
            <ColumnDefinition Width="33*"/>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="QueueCountDisplay" Text="队列数量: 0" Margin="30,30,0,0" Width="200" Height="22" VerticalAlignment="Top" HorizontalAlignment="Left" Grid.Column="1" FontSize="18"/>
        <TextBlock x:Name="ScreenIndexDisplay" Text="screen: 0" Margin="30,60,0,0" Width="200" Height="22" VerticalAlignment="Top" HorizontalAlignment="Left" Grid.Column="1" FontSize="18"/>

        <Button x:Name="测试通知显示" Content="显示通知" HorizontalAlignment="Left" Margin="30,100,0,0" 
                VerticalAlignment="Top"
                Click="ShowNotificationButton_Click" Grid.Column="1" FontSize="18" 
                />
        <Button x:Name="button1" Content="启动监听18880端口"
                HorizontalAlignment="Left" Margin="30,130,0,0" 
                VerticalAlignment="Top" Click="StartButton_Click" Grid.Column="1" FontSize="18" />

        <ScrollViewer Margin="0,10,0,0" Width="400" HorizontalAlignment="Left"
                         HorizontalScrollBarVisibility="Hidden" 
                            VerticalScrollBarVisibility="Hidden" 
                      >
            <TextBlock x:Name="MessageDisplay"
                       TextWrapping="Wrap"
                       FontSize="14"
                       Text="Waiting for messages..."/>
        </ScrollViewer>
    </Grid>

</Window>
